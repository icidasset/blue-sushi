# blue-sushi greetd configuration
# Noctalia Greeter (from Terra), a graphical login screen matching the shell.
# Fedora's greetd package creates the "greetd" user for greeters.

[terminal]
vt = 1

[default_session]
command = "/usr/bin/noctalia-greeter-session"
user = "greetd"